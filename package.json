{
  "name": "@hawapi/docs",
  "version": "1.3.1",
  "packageManager": "yarn@4.0.1",
  "scripts": {
    "build": "retype build && node scripts/js/update-colors.mjs",
    "watch": "retype start",
    "dev": "retype start",
    "clean": "rm -rf ./build"
  },
  "devDependencies": {
    "retypeapp": "^3.5.0"
  }
}
